# conda environment description
#
# Use:
#
#   conda env create -f environment.yaml
#
# to create an environment in which you can run the ERDDAP_datasets notebook.

name: erddap-datasets

channels:
  - defaults
  - conda-forge

dependencies:
  - bottleneck
  - hdf4
  - jupyter_contrib_nbextensions
  - lxml
  - netcdf4
  - notebook
  - pip
  - python=3.6
  - xarray

  - pip:
      - yapf
